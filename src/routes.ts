import express from "express";

const router = express.Router();

router.post("/users", async () => {});

router.get("/users", async () => {});

router.get("/users/userID", async () => {});

//router.post("/users/userID", async () => {});

export { router };

